@Library('my-library') _
pipeline {
    agent any  
     stages {
        stage('Example') {
            steps {
                sayHello("Huy","Gpay")
            }
        }
    }
}

pipeline {
    agent any  
     stages {
        stage('Example-2') {
            steps {
                sh 'echo Hello'
            }
        }
    }
}   

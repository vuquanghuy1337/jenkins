libraries {
     lib('pipeline-library-demo')
 }
pipeline {
    agent any  
     stages {
        stage('Example') {
            steps {
                sayHello("Huy","Gpay")
            }
        }
    }
}
   
